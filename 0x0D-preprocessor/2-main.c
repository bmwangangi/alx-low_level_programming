#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
		/* Prints the name of the file it was compiled from */
		printf(__FILE__);
			printf("\n");
				return (0);
}
